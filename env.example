# Anki Card Writing Assistant 环境变量配置示例
# 复制此文件为 .env 并修改相应值

# ====================
# LLM 配置
# ====================

# API 密钥（必需）
LLM_API_KEY=your_openai_api_key_here

# API 基础 URL（可选，默认 OpenAI 官方）
LLM_BASE_URL=https://api.openai.com/v1

# 模型名称（可选，默认 gpt-3.5-turbo）
LLM_MODEL=gpt-3.5-turbo

# 温度参数（可选，默认 0.7）
LLM_TEMPERATURE=0.7

# 最大 token 数（可选，默认 20000）
LLM_MAX_TOKENS=20000

# 超时时间（秒，可选，默认 30）
LLM_TIMEOUT=30

# ====================
# 生成配置
# ====================

# 默认难度（可选：easy/medium/hard，默认 medium）
GEN_DEFAULT_DIFFICULTY=medium

# 默认卡片数量（可选，默认 1）
GEN_DEFAULT_CARD_COUNT=1

# ====================
# 导出配置
# ====================

# 输出目录（可选，默认 output）
EXPORT_OUTPUT_DIR=output

# 模板目录（可选，默认 Card Template）
TEMPLATES_DIR=Card Template

# ====================
# Web 服务配置
# ====================

# 监听地址（可选，默认 0.0.0.0）
HOST=0.0.0.0

# 监听端口（可选，默认 5000）
PORT=5000

# 调试模式（可选：true/false，默认 false）
FLASK_DEBUG=false

# ====================
# 容器配置
# ====================

# 是否覆盖已有配置（可选：true/false，默认 true）
# 设为 false 可保留容器内手动修改的配置
OVERWRITE_CONFIG=true
